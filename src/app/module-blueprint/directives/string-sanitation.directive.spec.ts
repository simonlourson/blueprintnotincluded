import { StringSanitationDirective } from './string-sanitation.directive';

describe('StringSanitationDirective', () => {
  it('should create an instance', () => {
    const directive = new StringSanitationDirective();
    expect(directive).toBeTruthy();
  });
});
